t.select(1)

while true do
	
	for i=1,3 do
		if t.compare(1) then
			t.dig()
		end
		t.rt()
	end
	if t.compare(1) then
		t.dig()
	end

	local count = t.getItemCount(1)

	t.dropDn(count - 1)

	os.sleep(30)
end