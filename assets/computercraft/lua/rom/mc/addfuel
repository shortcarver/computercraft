-- used to keep a furnace full of fuel
-- place a chest below with fuel in it, and a furnace in front

while true do
	turtle.suckDown()
	local count = turtle.getItemCount(1)
	if count > 1 then
		turtle.drop(count-1)
	end
	os.sleep(80)
end

