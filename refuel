os.loadAPI("/rom/mcarver/dbc")

function maintainLevel(level)
	while turtle.getFuelLevel() < level and turtle.getItemCount(1) > 0 do
		turtle.select(1)
		turtle.refuel(1)
	end

	if turtle.getItemCount(1) <= 10 then
		dbc.orderItem("charcoal")
	end
end